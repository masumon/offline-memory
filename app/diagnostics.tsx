import { useMemo, useState } from 'react';
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import { useSQLiteContext } from 'expo-sqlite';
import { router } from 'expo-router';
import { runDiagnostics, type DiagnosticsReport } from '../src/services/diagnostics-service';
import { collectRuntimeHealth, type RuntimeHealth } from '../src/services/runtime-health-service';
import { useAppPreferences } from '../src/app/AppPreferences';
import { AppIcon } from '../src/ui/AppIcon';
import { AppState } from '../src/ui/AppSurface';
import { elevation, spacing, type ThemeColors } from '../src/theme';
import { formatBangladeshDateTime } from '../src/i18n/date-time';

export default function DiagnosticsScreen(){const db=useSQLiteContext();const{colors,language}=useAppPreferences();const bn=language==='bn';const styles=useMemo(()=>makeStyles(colors),[colors]);const[report,setReport]=useState<DiagnosticsReport|null>(null);const[runtime,setRuntime]=useState<RuntimeHealth|null>(null);const[running,setRunning]=useState(false);const[error,setError]=useState<string|null>(null);const check=async()=>{setRunning(true);setError(null);try{const[diagnostics,health]=await Promise.all([runDiagnostics(db),collectRuntimeHealth(db)]);setReport(diagnostics);setRuntime(health)}catch{setError(bn?'ডিভাইস ডায়াগনস্টিকস চালানো যায়নি':'Unable to run device diagnostics');setReport(null);setRuntime(null)}finally{setRunning(false)}};const copy=bn?{back:'ফিরুন',eyebrow:'M8 · QA',title:'ডিভাইস ডায়াগনস্টিকস',subtitle:'সার্ভারে ডেটা না পাঠিয়ে লোকাল ডেটাবেস ও Android রিমাইন্ডার লেয়ার যাচাই করুন।',run:'ডায়াগনস্টিকস চালান',runtime:'রানটাইম',platform:'প্ল্যাটফর্ম',database:'ডেটাবেস',readable:'পাঠযোগ্য',unavailable:'অনুপলব্ধ',notifications:'নোটিফিকেশন',scheduled:'নির্ধারিত রিমাইন্ডার',passed:'সব চেক পাস',action:'অ্যাকশন প্রয়োজন',hint:'Android build ইনস্টল করার পর ডিভাইস পরিবেশ যাচাই করতে চেকটি চালান।',retry:'আবার চেষ্টা করুন'}:{back:'Back',eyebrow:'M8 · QA',title:'Device diagnostics',subtitle:'Verify the local database and Android reminder layer without sending data to a server.',run:'Run diagnostics',runtime:'Runtime',platform:'Platform',database:'Database',readable:'Readable',unavailable:'Unavailable',notifications:'Notifications',scheduled:'Scheduled reminders',passed:'All checks passed',action:'Action required',hint:'Run the check after installing the Android build to validate the device environment.',retry:'Retry'};return <ScrollView style={styles.container} contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}><Pressable accessibilityRole="button" onPress={()=>router.back()} style={({pressed})=>StyleSheet.flatten([styles.back,pressed&&styles.pressed])}><AppIcon name="arrow-left" size={20} color={colors.primary}/><Text style={styles.backText}>{copy.back}</Text></Pressable><View style={styles.titleRow}><View style={styles.titleIcon}><AppIcon name="heart-pulse" size={27} color={colors.primary}/></View><View style={styles.titleCopy}><Text style={styles.eyebrow}>{copy.eyebrow}</Text><Text style={styles.title}>{copy.title}</Text></View></View><Text style={styles.subtitle}>{copy.subtitle}</Text><Pressable disabled={running} accessibilityRole="button" accessibilityState={{disabled:running,busy:running}} onPress={()=>void check()} style={({pressed})=>StyleSheet.flatten([styles.primaryButton,running&&styles.disabled,pressed&&styles.pressed])}>{running?<ActivityIndicator color={colors.onPrimary}/>:<><AppIcon name="play-circle-outline" size={19} color={colors.onPrimary}/><Text style={styles.primaryText}>{copy.run}</Text></>}</Pressable>{error?<AppState title={bn?'ডায়াগনস্টিকস ব্যর্থ হয়েছে':'Diagnostics failed'} description={bn?'লোকাল ডিভাইস চেকটি সম্পন্ন হয়নি।':'The local device checks could not be completed.'} icon="alert-circle-outline" actionLabel={copy.retry} onAction={()=>void check()}/>:null}{runtime?<View style={styles.runtime}><View style={styles.runtimeHeading}><AppIcon name="chip" size={21} color={colors.primary}/><Text style={styles.runtimeTitle}>{copy.runtime}</Text></View><Text style={styles.runtimeText}>{copy.platform}: {runtime.platform}</Text><Text style={styles.runtimeText}>{copy.database}: {runtime.databaseReadable?copy.readable:copy.unavailable}</Text><Text style={styles.runtimeText}>{copy.notifications}: {runtime.notifications}</Text><Text style={styles.runtimeText}>{copy.scheduled}: {runtime.scheduledNotificationCount}</Text></View>:null}{report?<View style={styles.results}><View style={styles.summaryRow}><AppIcon name={report.ok?'check-circle-outline':'alert-circle-outline'} size={24} color={report.ok?colors.success:colors.danger}/><Text style={[styles.summary,report.ok?styles.ok:styles.fail]}>{report.ok?copy.passed:copy.action}</Text></View>{report.checks.map(item=><View key={item.id} style={styles.card}><View style={styles.cardHeading}><AppIcon name={item.ok?'check-circle-outline':'alert-outline'} size={20} color={item.ok?colors.success:colors.warning}/><Text style={styles.cardTitle}>{item.label}</Text></View><Text style={styles.cardText}>{item.detail}</Text></View>)}<Text style={styles.timestamp}>{formatBangladeshDateTime(report.generatedAt,language)}</Text></View>:!error?<Text style={styles.hint}>{copy.hint}</Text>:null}</ScrollView>}
function makeStyles(colors:ThemeColors){return StyleSheet.create({container:{flex:1,backgroundColor:colors.background},content:{paddingHorizontal:spacing.lg,paddingTop:spacing.md,paddingBottom:spacing.xxl},back:{minHeight:48,flexDirection:'row',alignItems:'center',gap:4,marginBottom:spacing.lg},backText:{color:colors.primary,fontSize:16,fontWeight:'800'},titleRow:{flexDirection:'row',alignItems:'center',gap:spacing.md},titleIcon:{width:52,height:52,borderRadius:16,backgroundColor:colors.surfaceMuted,alignItems:'center',justifyContent:'center'},titleCopy:{flex:1,minWidth:0},eyebrow:{color:colors.primary,fontSize:12,fontWeight:'900',letterSpacing:1.2},title:{color:colors.textPrimary,fontSize:30,fontWeight:'900',marginTop:2},subtitle:{color:colors.textSecondary,fontSize:14,lineHeight:21,marginTop:spacing.md},primaryButton:{minHeight:50,borderRadius:14,backgroundColor:colors.primary,justifyContent:'center',alignItems:'center',marginTop:spacing.lg,flexDirection:'row',gap:4},primaryText:{color:colors.onPrimary,fontWeight:'800'},disabled:{opacity:.6},runtime:{marginTop:spacing.lg,padding:spacing.md,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border,borderRadius:16,...elevation.card},runtimeHeading:{flexDirection:'row',alignItems:'center',gap:spacing.sm,marginBottom:spacing.sm},runtimeTitle:{color:colors.textPrimary,fontSize:16,fontWeight:'900'},runtimeText:{color:colors.textSecondary,fontSize:14,lineHeight:22},results:{marginTop:spacing.lg},summaryRow:{flexDirection:'row',alignItems:'center',gap:spacing.sm,marginBottom:spacing.md},summary:{fontSize:18,fontWeight:'900'},ok:{color:colors.success},fail:{color:colors.danger},card:{backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border,borderRadius:16,padding:spacing.md,marginBottom:spacing.sm,...elevation.card},cardHeading:{flexDirection:'row',alignItems:'center',gap:spacing.sm},cardTitle:{color:colors.textPrimary,fontSize:15,fontWeight:'800'},cardText:{color:colors.textSecondary,fontSize:14,lineHeight:21,marginTop:spacing.xs},timestamp:{color:colors.textMuted,fontSize:12,marginTop:spacing.sm},hint:{color:colors.textSecondary,fontSize:14,lineHeight:21,marginTop:spacing.xl,textAlign:'center'},pressed:{opacity:.78}})}
